VIBEPROXY_BASE_URL=http://localhost:8317/v1
# Must match a model your VibeProxy actually serves — list them with:
#   curl -s http://localhost:8317/v1/models
# (model availability depends on which provider subscriptions you've authenticated)
VIBEPROXY_MODEL=gpt-5.4
VIBEPROXY_API_KEY=dummy-not-used
